server:
  port: 8082

spring:

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/critter_chronologer
    username: username
    password: password

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format-sql: true